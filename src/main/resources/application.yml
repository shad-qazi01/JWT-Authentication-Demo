
spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/Application
#    username: postgres
#    password: root
#    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate / none (use as per need)
    show-sql: true
    properties:
      hibernate:
        default_schema: Authentication
        format-sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        id:
          new_generator_mappings: false  # Native PostgreSQL sequences



  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 511425e46d7fed
    password: 14c6ab6e4e0709
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#--------

app:
  jwt:
    secret: "change-this-256-bit-long-secret-key-for-demo-only-1234567890abcd" # demo only
    expiry-minutes: 30
  cors:
    allowed-origins: "http://localhost:5173"

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE